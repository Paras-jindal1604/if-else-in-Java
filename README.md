# If-Else Statements in Java

This repository provides an in-depth look at using `if-else` statements in Java, complete with a variety of example questions to enhance your understanding.

## Description

The `if-else` statement in Java allows you to execute different blocks of code based on certain conditions.
This repository contains examples and exercises that demonstrate how to effectively use `if-else` statements to handle different scenarios.
